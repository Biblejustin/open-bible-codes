# Biblical Calendar

Term list:

- `terms/biblical_calendar.csv`

Purpose:

- Declare Hebrew month names and pre-exilic month names for screening.
- Keep these distinct from date gematria encodings.

Not run yet:

- This file is intentionally not wired into `protocols/public_baseline.toml`.

Cautions:

- Short month names such as Av and Ziv are noisy and may be skipped by default settings.
- Calendar names are screening terms, not claims of significance.
